export default class Room {
    constructor(roomId, user1id, user2id, fen) {
        this.roomId = roomId;
        this.user1id = user1id;
        this.user2id = user2id;
        this.fen = fen;
    }
}